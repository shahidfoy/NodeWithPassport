<!DOCTYPE html>
<html>
<head>
    <title>LoginApp</title>
    <link rel="stylesheet" href="/css/bootstrap.css" />
    <link rel="stylesheet" href="/css/style.css" />

    <script defer src="/js/fontawesome-all.js"></script>
</head>
<body>
<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <a class="navbar-brand" href="/">Login App <i class="fas fa-camera-retro"></i></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExampleDefault">

        <ul class="navbar-nav mr-auto">
            {{#if user }}
            <li class="nav-item active">
                <a class="nav-link" href="/dashboard">Dashboard <span class="sr-only">(current)</span></a>
            </li>
            {{/if}}
        </ul>


        <ul class="navbar-nav navbar-right">
            {{#if user }}
                <li class="nav-item">
                    <a class="nav-link" href="/users/logout">Logout <i class="fas fa-lock"></i></a>
                </li>
            {{else}}
                <li class="nav-item">
                    <a class="nav-link" href="/users/login">Login <i class="fas fa-sign-in-alt"></i></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/users/register">Register <i class="fas fa-anchor"></i></a>
                </li>
            {{/if}}
        </ul>
    </div>
</nav>

<div class="row">
    <div class="container">
        {{#if success_msg}}
            <br>
            <div class="alert alert-success">{{success_msg}}</div>
        {{/if}}
        {{#if error_msg}}
            <br>
            <div class="alert alert-danger">{{error_msg}}</div>
        {{/if}}
        {{#if error}}
            <br>
            <div class="alert alert-danger">{{error}}</div>
        {{/if}}
    </div>
</div>

{{{ body }}}

<footer class="container">
    <p>&copy; Company 2018</p>
</footer>

    <script
            src="https://code.jquery.com/jquery-3.2.1.min.js"
            integrity="sha256-hwg4gsxgFZhOsEEamdOYGBf13FyQuiTwlAQgxVSNgt4="
            crossorigin="anonymous"></script>
    <script src="/js/popper.min.js"></script>
    <script src="/js/bootstrap.js"></script>
</body>
</html>